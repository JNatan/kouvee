<h2>Detail Transaksi</h2>

<p><strong>ID Transaksi:</strong> <%= trx.id_transaksi %></p>
<p><strong>Customer:</strong> <%= trx.nama_customer %></p>
<p><strong>Pegawai:</strong> <%= trx.nama_pegawai %></p>

<p><strong>Total Asli:</strong> Rp <%= trx.total.toLocaleString() %></p>
<p><strong>Diskon:</strong> Rp <%= trx.diskon.toLocaleString() %></p>

<h3>Total Setelah Diskon: Rp <%= trx.total_setelah_diskon.toLocaleString() %></h3>

<p><strong>Sudah Dibayar:</strong> Rp <%= trx.bayar_total.toLocaleString() %></p>
<p><strong>Sisa:</strong> Rp <%= trx.sisa.toLocaleString() %></p>

<h3>Status Pembayaran:</h3>
<% if (trx.status_pembayaran == 1) { %>
  <span class="badge badge-success">LUNAS</span>
<% } else { %>
  <span class="badge badge-danger">BELUM LUNAS</span>
<% } %>

<hr>

<h3>Produk</h3>
<% detail_produk.forEach(d => { %>
  <p><%= d.nama_produk %> x <%= d.jumlah %></p>
<% }) %>

<h3>Layanan</h3>
<% detail_layanan.forEach(d => { %>
  <p><%= d.nama_layanan %></p>
<% }) %>

<a href="/pembayaran" class="btn">Kembali</a>